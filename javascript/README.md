# Odin Project JavaScript - Last Three Projects

This folder contains implementations for the final three projects from the JavaScript path:

1. **HashMap** (`hashmap/hashMap.js`)
2. **Binary Search Tree** (`binary-search-tree/tree.js`)
3. **Knight Travails** (`knights-travails/knightTravails.js`)

## Run quick demos

From repository root:

```bash
node javascript/runDemos.js
```
